import { PlaceholderPage } from "@/components/placeholder-page";

export default function XepLichPage() {
  return (
    <PlaceholderPage
      icon="🗓️"
      title="Xếp lịch"
      pha="Pha 2"
      note="Bảng xếp lịch (Planning Board) gán lệnh lên máy + 3 trợ lý quyết định (cảnh báo trễ, gom màu/khổ, tải máy)."
    />
  );
}
